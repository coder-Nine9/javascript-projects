# 🧮 JavaScript Calculator

A simple and interactive **calculator web application** built using **vanilla JavaScript**.  
It supports basic arithmetic operations with a clean and responsive UI.

## 📌 Features
- Addition, subtraction, multiplication, and division
- Parentheses support
- Decimal numbers
- Clear all (`C`)
- Delete last character (`Del`)
- Error handling for invalid expressions

## 🛠️ Technologies Used
- HTML
- CSS
- JavaScript (Vanilla JS)

## 📂 Project Structure
- `index.html` — Calculator layout
- `style.css` — Styling and layout
- `script.js` — Calculator logic and event handling

## ⚙️ How It Works
- Button clicks are handled using **event delegation**
- Each button updates the display dynamically
- Expressions are evaluated when `=` is pressed
- Errors are caught and displayed safely

## ▶️ How to Run
1. Download or clone the project
2. Open `index.html` in your browser
3. Start calculating 🚀

## 🎯 Learning Goals
- DOM manipulation
- Event delegation
- String manipulation
- Error handling with `try / catch`
- Building real interactive UI logic

## ⚠️ Notes
This calculator uses JavaScript’s `eval()` for expression evaluation.  
It is suitable for learning purposes and will be improved in future versions.

## 🚀 Future Improvements
- Keyboard support
- Replace `eval()` with a custom parser
- History of calculations
- Better mobile responsiveness

## 👤 Author
**Abdelaziz El Khanoussi**
